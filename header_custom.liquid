{% schema %}
{
  "name": "Custom Header",
  "tag": "header",
  "class": "section-header",
  "settings": [
    {
      "type": "header",
      "content": "Logo"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Desktop Logo Image",
      "info": "Recommended dimensions: 300px width x 120px height (Max displayed height: 60px)"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 300,
      "step": 10,
      "default": 150,
      "unit": "px",
      "label": "Desktop Logo Width"
    },
    {
      "type": "header",
      "content": "Mobile Logo"
    },
    {
      "type": "image_picker",
      "id": "mobile_logo",
      "label": "Mobile Logo Image",
      "info": "Recommended dimensions: 200px width x 80px height"
    },
    {
      "type": "range",
      "id": "mobile_logo_width",
      "min": 30,
      "max": 200,
      "step": 5,
      "default": 100,
      "unit": "px",
      "label": "Mobile Logo Width"
    },
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Main Menu",
      "default": "main-menu"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "header_bg",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "header_text",
      "label": "Text Color",
      "default": "#444444"
    },
    {
      "type": "color",
      "id": "header_heading",
      "label": "Heading & Link Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "header_border",
      "label": "Border Color",
      "default": "#D8D6CF"
    },
    {
      "type": "color",
      "id": "header_accent",
      "label": "Accent Color",
      "default": "#E5611A"
    }
  ],
  "blocks": [
    {
      "type": "featured_item",
      "name": "Drawer featured item",
      "settings": [
        {
          "type": "select",
          "id": "item_source",
          "label": "Item source",
          "default": "product",
          "options": [
            {
              "value": "product",
              "label": "Product"
            },
            {
              "value": "collection",
              "label": "Collection"
            },
            {
              "value": "custom",
              "label": "Custom"
            }
          ]
        },
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Custom image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Custom title"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Custom link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Header"
    }
  ]
}
{% endschema %}

{% style %}
  :root {
    --header-bg: {{ section.settings.header_bg }};
    --header-text: {{ section.settings.header_text }};
    --header-heading: {{ section.settings.header_heading }};
    --header-border: {{ section.settings.header_border }};
    --header-accent: {{ section.settings.header_accent }};
    --header-height-desktop: 80px;
    --header-height-mobile: auto;
  }

  .section-header {
    position: sticky;
    top: 0;
    z-index: 100000 !important;
  }

  .custom-header {
    background-color: var(--header-bg);
    color: var(--header-text);
    box-shadow: 0 1px 0 var(--header-border);
    font-family: 'Inter', sans-serif;
  }

  .header-container {
    max-width: 1600px;
    margin: 0 auto;
    padding: 0 20px;
    contain: layout style;
  }

  /* Icons */
  .icon {
    width: 20px;
    height: 20px;
    fill: currentColor;
    vertical-align: middle;
  }
  .icon-md {
    width: 24px;
    height: 24px;
  }

  /* Desktop Layout */
  .header-desktop {
    display: none;
    height: var(--header-height-desktop);
    align-items: center;
    justify-content: space-between;
  }

  .header-left {
    display: flex;
    align-items: center;
    gap: 25px;
    flex: 1;
  }

  .header-links {
    display: flex;
    gap: 24px;
  }

  .header-link {
    text-decoration: none;
    color: var(--header-heading);
    font-size: 15px;
    font-weight: 500;
    white-space: nowrap;
    font-family: 'Sora', 'Manrope', sans-serif;
    transition: color 0.2s;
  }

  .header-link:hover {
    color: var(--header-accent);
  }

  .header-center {
    flex: 0 0 auto;
    text-align: center;
  }

  .header-logo {
    display: block;
    line-height: 0;
  }

  .header-logo img {
    display: block;
    height: auto;
    max-height: 60px;
    object-fit: contain;
  }

  .header-logo-text {
    font-size: 24px;
    font-weight: 600;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--header-heading);
    letter-spacing: 0.5px;
    font-family: 'Sora', 'Manrope', sans-serif;
  }

  .header-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 20px;
    flex: 1;
  }

  .search-bar-desktop {
    position: relative;
    width: 280px;
  }

  .search-input {
    width: 100%;
    padding: 10px 40px 10px 16px;
    border-radius: 30px;
    border: 1px solid #0E1D3C;
    background-color: transparent;
    font-size: 14px;
    outline: none;
    transition: all 0.2s;
    color: var(--header-text);
    font-family: 'Inter', sans-serif;
  }

  .search-input:focus {
    background-color: transparent;
    border-color: #0E1D3C;
  }

  .search-icon-btn {
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    color: var(--header-text);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .action-btn {
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
    color: var(--header-heading);
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 40px;
    min-height: 40px;
    position: relative;
    transition: color 0.2s;
  }
  
  .action-btn:hover {
    color: var(--header-accent);
  }

  .cart-count-bubble {
    position: absolute;
    top: 4px;
    right: 4px;
    background-color: var(--header-accent);
    color: #fff;
    font-size: 10px;
    font-weight: 700;
    border-radius: 50%;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
  }

  /* Mobile Layout */
  .header-mobile {
    display: flex;
    flex-direction: column;
    padding: 12px 0;
    gap: 12px;
  }

  .mobile-top-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
  }

  .mobile-left, .mobile-right {
    display: flex;
    gap: 8px;
    align-items: center;
    flex: 1;
  }

  .mobile-right {
    justify-content: flex-end;
  }

  .mobile-center {
    flex: 0 0 auto;
  }

  .search-bar-mobile {
    width: 100%;
    position: relative;
  }

  /* Drawer Menu */
  .drawer-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    z-index: 2147483647 !important;
    visibility: hidden;
    opacity: 0;
    transition: opacity 0.3s ease-out;
    will-change: opacity, visibility;
    pointer-events: none;
    overscroll-behavior: contain;
  }

  .drawer-menu.is-open {
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
  }

  .drawer-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.4);
    backdrop-filter: blur(2px);
  }

  .drawer-content {
    position: absolute;
    top: 0;
    right: 0;
    width: 85%;
    max-width: 400px;
    height: 100%;
    background: var(--header-bg);
    transform: translateX(100%);
    transition: transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
    display: flex;
    flex-direction: column;
    box-shadow: -2px 0 10px rgba(0,0,0,0.1);
    will-change: transform;
  }

  .drawer-menu.is-open .drawer-content {
    transform: translateX(0);
  }

  .drawer-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 20px;
    border-bottom: 1px solid var(--header-border);
  }

  .drawer-body {
    flex: 1;
    overflow-y: auto;
    padding: 20px;
  }

  .drawer-featured {
    display: flex;
    flex-direction: row;
    gap: 16px;
    margin-bottom: 24px;
    overflow-x: auto;
    padding-bottom: 12px;
    scrollbar-width: thin;
    -webkit-overflow-scrolling: touch;
    contain: content;
  }

  .drawer-featured::-webkit-scrollbar {
    height: 4px;
  }
  .drawer-featured::-webkit-scrollbar-track {
    background: transparent;
  }
  .drawer-featured::-webkit-scrollbar-thumb {
    background: var(--header-border);
    border-radius: 4px;
  }

  .drawer-featured-item {
    text-align: center;
    text-decoration: none;
    color: inherit;
    flex: 0 0 120px;
    min-width: 120px;
  }

  .drawer-featured-img {
    width: 100%;
    aspect-ratio: 1;
    object-fit: cover;
    border-radius: 4px;
    margin-bottom: 8px;
    background: #f4f3ef;
  }

  .drawer-featured-title {
    font-weight: 500;
    font-size: 13px;
    font-family: 'Inter', sans-serif;
  }

  .drawer-nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .drawer-nav-item {
    border-bottom: 1px solid var(--header-border);
  }

  .drawer-nav-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 14px 0;
    text-decoration: none;
    color: var(--header-heading);
    font-size: 16px;
    font-weight: 500;
    font-family: 'Sora', 'Manrope', sans-serif;
  }

  @media (min-width: 990px) {
    .header-desktop {
      display: flex;
    }
    .header-mobile {
      display: none;
    }
  }
{% endstyle %}

<div class="custom-header">
  <div class="header-container">
    
    <!-- Desktop Header -->
    <div class="header-desktop">
      <div class="header-left">
        <!-- Hamburger Menu (moved to left) -->
        <button class="action-btn js-drawer-toggle" aria-label="Menu">
          <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M4 6h16v2H4zm0 5h16v2H4zm0 5h16v2H4z"/></svg>
        </button>
        <nav class="header-links">
          {% if section.settings.menu != blank %}
            {% for link in linklists[section.settings.menu].links %}
              <a href="{{ link.url }}" class="header-link">{{ link.title }}</a>
            {% endfor %}
          {% endif %}
        </nav>
      </div>

      <div class="header-center">
        <a href="/" class="header-logo">
          {% if section.settings.logo != blank %}
            {%- assign logo_width_2x = section.settings.logo_width | times: 2 -%}
            {{ section.settings.logo | image_url: width: logo_width_2x | image_tag: width: section.settings.logo_width, loading: 'eager', alt: shop.name }}
          {% else %}
            <span class="header-logo-text">{{ shop.name }}</span>
          {% endif %}
        </a>
      </div>

      <div class="header-right">
        <form action="{{ routes.search_url }}" method="get" role="search" class="search-bar-desktop">
          <input type="text" name="q" class="search-input" placeholder="Search">
          <button type="submit" class="search-icon-btn">
            <svg class="icon" viewBox="0 0 24 24"><path d="M21.71 20.29L18 16.61A9 9 0 1 0 16.61 18l3.68 3.68a1 1 0 0 0 1.42 0 1 1 0 0 0 0-1.42zM11 18a7 7 0 1 1 7-7 7 7 0 0 1-7 7z"/></svg>
          </button>
        </form>
        
        <a href="{{ routes.cart_url }}" class="action-btn" aria-label="Cart">
          <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/></svg>
          {%- if cart.item_count > 0 -%}
            <span class="cart-count-bubble">{{ cart.item_count }}</span>
          {%- endif -%}
        </a>
        
        <a href="{{ routes.account_url }}" class="action-btn" aria-label="Account">
          <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </a>
      </div>
    </div>

    <!-- Mobile Header -->
    <div class="header-mobile">
      <div class="mobile-top-row">
        <div class="mobile-left">
          <!-- Hamburger moved to left on mobile -->
          <button class="action-btn js-drawer-toggle" aria-label="Menu">
            <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M4 6h16v2H4zm0 5h16v2H4zm0 5h16v2H4z"/></svg>
          </button>
          <a href="{{ routes.account_url }}" class="action-btn" aria-label="Account">
            <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
          </a>
        </div>

        <div class="mobile-center">
          <a href="/" class="header-logo">
            {%- liquid
              assign mobile_logo_image = section.settings.mobile_logo | default: section.settings.logo
              assign mobile_logo_width = section.settings.mobile_logo_width
              assign mobile_logo_width_2x = mobile_logo_width | times: 2
            -%}
            {% if mobile_logo_image != blank %}
              {{ mobile_logo_image | image_url: width: mobile_logo_width_2x | image_tag: width: mobile_logo_width, loading: 'eager', alt: shop.name, style: 'height: auto; width: auto;' }}
            {% else %}
              <span class="header-logo-text">{{ shop.name }}</span>
            {% endif %}
          </a>
        </div>

        <div class="mobile-right">
          <a href="{{ routes.cart_url }}" class="action-btn" aria-label="Cart">
            <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/></svg>
            {%- if cart.item_count > 0 -%}
              <span class="cart-count-bubble">{{ cart.item_count }}</span>
            {%- endif -%}
          </a>
        </div>
      </div>

      <div class="mobile-bottom-row">
        <form action="{{ routes.search_url }}" method="get" role="search" class="search-bar-mobile">
          <input type="text" name="q" class="search-input" placeholder="Search">
          <button type="submit" class="search-icon-btn">
            <svg class="icon" viewBox="0 0 24 24"><path d="M21.71 20.29L18 16.61A9 9 0 1 0 16.61 18l3.68 3.68a1 1 0 0 0 1.42 0 1 1 0 0 0 0-1.42zM11 18a7 7 0 1 1 7-7 7 7 0 0 1-7 7z"/></svg>
          </button>
        </form>
      </div>
    </div>
  </div>

  <!-- Drawer Menu -->
  <div class="drawer-menu" id="DrawerMenu">
    <div class="drawer-overlay js-drawer-toggle"></div>
    <div class="drawer-content">
      <div class="drawer-header">
        <a href="/" class="header-logo">
          {%- liquid
            assign mobile_logo_image = section.settings.mobile_logo | default: section.settings.logo
            assign mobile_logo_width = section.settings.mobile_logo_width
            assign mobile_logo_width_2x = mobile_logo_width | times: 2
          -%}
          {% if mobile_logo_image != blank %}
            {{ mobile_logo_image | image_url: width: mobile_logo_width_2x | image_tag: width: mobile_logo_width, loading: 'lazy', alt: shop.name, style: 'height: auto; width: auto;' }}
          {% else %}
            <span class="header-logo-text">{{ shop.name }}</span>
          {% endif %}
        </a>
        <button class="action-btn js-drawer-toggle" aria-label="Close menu">
          <svg class="icon icon-md" viewBox="0 0 24 24"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/></svg>
        </button>
      </div>

      <div class="drawer-body">
        <div class="drawer-featured">
          {% for block in section.blocks %}
            {% if block.type == 'featured_item' %}
              {% liquid
                assign item_image = nil
                assign item_title = nil
                assign item_url = '#'

                if block.settings.item_source == 'product' and block.settings.product != blank
                  assign item_image = block.settings.product.featured_image
                  assign item_title = block.settings.product.title
                  assign item_url = block.settings.product.url
                elsif block.settings.item_source == 'collection' and block.settings.collection != blank
                  assign item_image = block.settings.collection.image
                  assign item_title = block.settings.collection.title
                  assign item_url = block.settings.collection.url
                else
                  assign item_image = block.settings.image
                  assign item_title = block.settings.title
                  assign item_url = block.settings.link
                endif
              %}
              <a href="{{ item_url | default: '#' }}" class="drawer-featured-item" {{ block.shopify_attributes }}>
                {% if item_image != blank %}
                  <img
                    src="{{ item_image | image_url: width: 400, height: 400, crop: 'center' }}"
                    alt="{{ item_title | escape }}"
                    class="drawer-featured-img"
                  >
                {% else %}
                  {{ 'product-' | append: forloop.index | placeholder_svg_tag: 'drawer-featured-img' }}
                {% endif %}
                {% if item_title != blank %}
                  <div class="drawer-featured-title">{{ item_title }}</div>
                {% endif %}
              </a>
            {% endif %}
          {% endfor %}
        </div>

        {% if section.settings.menu != blank and linklists[section.settings.menu].links.size > 0 %}
          <ul class="drawer-nav-list">
            {% for link in linklists[section.settings.menu].links %}
              <li class="drawer-nav-item">
                <a href="{{ link.url }}" class="drawer-nav-link">
                  {{ link.title }}
                </a>
              </li>
            {% endfor %}
          </ul>
        {% endif %}
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const drawer = document.getElementById('DrawerMenu');
    const toggleBtns = document.querySelectorAll('.js-drawer-toggle');
    let isResizeScheduled = false;
    
    function setDrawerState(isOpen) {
      if (isOpen) {
        drawer.classList.add('is-open');
        document.body.style.overflow = 'hidden';
      } else {
        drawer.classList.remove('is-open');
        document.body.style.overflow = '';
      }
      
      toggleBtns.forEach(btn => {
        btn.setAttribute('aria-expanded', isOpen);
      });
      drawer.setAttribute('aria-hidden', !isOpen);
    }

    function closeDrawer() {
      setDrawerState(false);
    }

    function openDrawer() {
      setDrawerState(true);
    }

    closeDrawer();

    toggleBtns.forEach(btn => {
      btn.addEventListener('click', function() {
        if (drawer.classList.contains('is-open')) {
          closeDrawer();
        } else {
          openDrawer();
        }
      });
    });

    window.addEventListener('resize', function() {
      if (!isResizeScheduled) {
        window.requestAnimationFrame(function() {
          if (window.innerWidth >= 990 && drawer.classList.contains('is-open')) {
            closeDrawer();
          }
          isResizeScheduled = false;
        });
        isResizeScheduled = true;
      }
    }, { passive: true });

    document.addEventListener('keydown', function(event) {
      if (event.key === 'Escape' && drawer.classList.contains('is-open')) {
        closeDrawer();
      }
    });
  });
</script>
